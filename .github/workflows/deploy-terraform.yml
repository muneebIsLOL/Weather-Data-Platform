name: Terraform CI/CD

on:
  push:
    branches:
      - "aws-migration"
    paths:
      - "terraform/environments/dev/**"
      - "terraform/environments/prod/**"
  pull_request:
    branches:
      - "main"

permissions:
  id-token: write
  contents: read
  security-events: write 
  pull-requests: write

jobs:
  code-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.15.8"
          terraform_wrapper: false

      - name: Validate Dev Code
        working-directory: terraform/environments/dev
        run: |
          terraform init -backend=false
          terraform fmt -check
          terraform validate

      - name: Validate Prod Code
        working-directory: terraform/environments/prod
        run: |
          terraform init -backend=false
          terraform fmt -check
          terraform validate

      - name: Generate Trivy SARIF Report
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: "config"
          scan-ref: "terraform/"
          format: "sarif"
          output: "trivy-results.sarif"
          exit-code: "0"

      - name: Block on High and Critical Vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: "config"
          scan-ref: "terraform/"
          format: "table"
          exit-code: "1" 
          severity: "HIGH,CRITICAL"
          skip-setup-trivy: true

      - name: Upload Trivy scan results to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: "trivy-results.sarif"

  deploy-dev:
    needs: code-quality
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.15.8"
          terraform_wrapper: false
          
      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/TerraformDevRole
          aws-region: "ap-south-1"
          audience: sts.amazonaws.com

      - name: Terraform Init
        working-directory: terraform/environments/dev
        run: |
          terraform init -reconfigure \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET }}" \
            -backend-config="key=terraform/dev/terraform.tfstate" \
            -backend-config="use_lockfile=true" \
            -backend-config="region=ap-south-1"

      - name: Terraform Plan
        working-directory: terraform/environments/dev
        if: github.event_name == 'pull_request'
        run: terraform plan -input=false -out=tfplan

      - name: Terraform Apply
        working-directory: terraform/environments/dev
        if: github.ref == 'refs/heads/aws-migration' && github.event_name == 'push'
        run: terraform apply -input=false -auto-approve
        
      - name: Make Logs Folder
        working-directory: terraform/environments/dev
        if: failure()
        run: |
          mkdir -p logs
          terraform show > logs/plan_error.txt

      - name: Upload Test Logs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: terraform-dev-logs
          path: terraform/environments/dev/logs/

  deploy-prod:
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.15.8"
          terraform_wrapper: false

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/TerraformProdRole
          aws-region: "ap-south-1"
          audience: sts.amazonaws.com

      - name: Terraform Init
        working-directory: terraform/environments/prod
        run: |
          terraform init -reconfigure \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET }}" \
            -backend-config="key=terraform/prod/terraform.tfstate" \
            -backend-config="use_lockfile=true" \
            -backend-config="region=ap-south-1"

      - name: Terraform Plan
        working-directory: terraform/environments/prod
        if: github.event_name == 'pull_request'
        run: terraform plan -input=false -out=tfplan

      - name: Terraform Apply
        working-directory: terraform/environments/prod
        if: github.ref == 'refs/heads/aws-migration' && github.event_name == 'push'
        run: terraform apply -input=false -auto-approve

      - name: Make Logs Folder
        working-directory: terraform/environments/prod
        if: failure()
        run: |
          mkdir -p logs
          terraform show > logs/plan_error.txt

      - name: Upload Test Logs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: terraform-prod-logs
          path: terraform/environments/prod/logs/
